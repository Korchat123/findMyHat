# เป็นเกมตาราง
# ใช้array2ชั้นจะได้ตาราง
# ต้องมีการรับค่าขนาดตาราง
# สร้าง class ใช้ attibute ไว้เก็บค่าที่เปลี่ยน
# ต้องมี loop ไหม? git

สร้างตัวรับข้อมูลดูก่อน รับขนาดตาราง จำนวนหลุม
้ต้องรับค่าแค่ได้แค่ตัวเลกต้องเช็คจนกว่าจะเป็นตัวเลข ใช้ loopหรือปกติหรือทำ recursiveเลยดี? เป็น function เลยดีกว่า


สร้างclass field รับค่าต่างๆของเกมนี้ ขนาด กระดานตำแหน่ง player hole และ goal 
ต้องใช้loop เก็บตำแหนงและแสดงตำแหน่งของของในarray
สร้างตารางได้แล้ว 
ลองปริ้นดู
สร้างได้เรียบร้อย ตอนสร้างก็ระบุตำแหน่งได้ใช้ if elseใส่ตำแหน่งของ player และ hole

สร้าง class อื่น แต่จะมีเหมือนกัน คือ ตำแหน่ง และ role ใช้ชื่อว่า bordElement

สร้าง object player ก่อน playerรับ คำสั่งคนเดียวถ้าใช้เป็น classรวมจะดีไหม ? สร้าง class แยก hole กับ playerและ extendมาดีกว่า?
ใช้อันเดียวกันดีกว่าไม่ต้องสร้าง เพิ่ม attibute เหมื่อนกัน

เริ่มrandomPOS เช็คกับ playerได้แต่จริงๆต้องเช็คหมดงั้นสร้างfunctionไว้เช็คขึ้นใหม่
เช็คได้สามารถใช้สำหรับplayerได้ไว้เจอของหรือหลุม ไว้เพิ่มอีกที
หลุมไม่ได้มีหลุมเดียว ใช้ array ในใส่หลุมและแก้การ checkใช้foreach
เริ่มเคลื่อนที่player
ใช้การปรับตำแหน่งอันเดิม setPos
เริ่มเขียน การ move
ติดปัญหา checkOccupie คืนค่า player เพราะโดนเช็คก่อนเกมไม่จบ
ทำการreturn ใส่ค่าจาก function move แทน 
เดินออกฝั่งซ้ายและล่างได้
แก้จากsetposไม่ได้เนื่องจากคนละ object class ต้องเรียกจาก field
สร้างตัวกัน ใo move แทน เสร็จ
